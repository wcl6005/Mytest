<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN"
"http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg width="1000.0000" height="1000.0000" viewBox="0.0000 0.0000 1000.0000 1000.0000"  version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" onload='Init(evt)'>
   <script type='text/ecmascript'><![CDATA[
          var SVGDocument = null;
          var SVGRoot = null;
          function Init(evt)
          {
                SVGDocument = evt.target.ownerDocument;
                 SVGRoot = SVGDocument.documentElement;
          }
         function Toggle(evt,targetId)
          {
                      var obj = evt.target;
         if (targetId)
         {
            obj = SVGDocument.getElementById(targetId);
         }
                id=obj.getAttributeNS(null, 'id');
                var myobj,i,myid,strs,params,Number,states,state,cur
                strs=id.split("(");
                params=strs[1].split(";");
                Number=strs[0];
                states=params[1];
                state=params[2];
                cur=params[3];
                states=parseInt(states);
                state=parseInt(state);
                if(state+1>=states)
                        state=0;
                else
                        state=state+1;
                for(i=0;i<states;i++)
                {
                        myid=Number+'('+params[0]+';'+states+';'+i+';'+cur+';'+params[4]+';'+params[5]+';'+params[6]+';'+params[7];
                        myobj=SVGDocument.getElementById(myid);
                        if(i==state)
                        {
                                myobj.setAttributeNS(null, 'visibility', 'visible');
                        }
                        else
                        {
                                myobj.setAttributeNS(null, 'visibility', 'hidden');
                        }
                }
        }
   ]]></script>
<ellipse transform="matrix(1.0000,0.0000,0.0000,1.0000,137.0000,71.0000)" cx="0" cy="0" rx="42.7200" ry="42.7200"  style="fill:none;stroke-opacity:1.0000;stroke-width:1.0000;stroke:#000000 "/>

</svg>